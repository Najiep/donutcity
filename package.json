{
  "name": "donut-city-portal-backend",
  "version": "2.0.0",
  "private": true,
  "description": "Production-ready Donut City FiveM portal, secured admin CMS, MySQL persistence, and live player status bridge",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20.6.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file=.env server.js",
    "check": "node --check server.js && node --check app.js && node --check admin.js && node --check scripts/smoke-test.js",
    "smoke": "node scripts/smoke-test.js"
  },
  "dependencies": {
    "express": "^4.22.2",
    "mysql2": "^3.11.5"
  }
}
